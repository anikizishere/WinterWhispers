@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="text-center">
    
    @if(Model.MyUser != null)
    {
        <h2 class="display-4">Hej @Model.MyUser.FirstName</h2>
        
        @if(Model.MyUser.Birthyear > 2006)
        {
            <h4>Tyvärr du kan inte komma vidare, du har inte åldern inne.</h4>
        }
        else
        {
            <h4>Hoppas du har kul på forumet.</h4>
        }     
    }   

    <p>Den här webbplatsen använder cookies/kakor som samlar in vissa uppgifter om dig. Läs mer i vår <a href="./Privacy">Integritetspolicy</a>.</p>
    
     @* Karusell  *@
    <div id="demo" class="carousel slide" data-bs-ride="carousel">

       @*  Indikatorer/prickar *@
        <div class="carousel-indicators">
            <button type="button" data-bs-target="#demo" data-bs-slide-to="0" class="active"></button>
            <button type="button" data-bs-target="#demo" data-bs-slide-to="1"></button>
            <button type="button" data-bs-target="#demo" data-bs-slide-to="2"></button>
        </div>

      @*    Bildspelet/karusellen  *@
        <div class="carousel-inner">
            <div class="carousel-item active">
                <img src="~/img/skiers-hands-carousel.jpg" alt="Skiers" class="d-block w-100">
            </div>
            <div class="carousel-item">
                <img src="~/img/woman-snowboarding-carousel.jpg" alt="Snowboard" class="d-block w-100">
            </div>
            <div class="carousel-item">
                <img src="~/img/tree-carousel.jpg" alt="Snowboard" class="d-block w-100">
            </div>
        </div>

        @* Vänster och höger kontroller/ikoner  *@
        <button class="carousel-control-prev" type="button" data-bs-target="#demo" data-bs-slide="prev">
            <span class="carousel-control-prev-icon"></span>
        </button>
        <button class="carousel-control-next" type="button" data-bs-target="#demo" data-bs-slide="next">
            <span class="carousel-control-next-icon"></span>
        </button>
    </div>

    <h1 class="display-4">Välkommen till Winter Whispers </h1>

    <div style="text-align: center;">
        <p>
            Ett forum för skidåkning och allt där i kring! Här samlas passionerade skidåkare från hela världen för att dela med sig av sina erfarenheter, tips och inspiration.<br/>
            Oavsett om du är nybörjare eller erfaren åkare, hittar du en plats här för att diskutera allt som rör skidåkning.
        </p>

    </div>

    <h4>Antal publicerande inlägg sedan start: @Model.Topics.Count() st.</h4>
    <ul class="no-bullets">
        @foreach (var topic in Model.Topics)
        {
            <li class="highlight">@topic.Name @topic.Id</li>
        }
    </ul>

</div>
